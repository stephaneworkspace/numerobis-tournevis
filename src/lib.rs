pub mod numerologie;

pub fn str_vers_nombre(str: &str) -> i32 {
    numerologie::str_vers_nombre(str)
}

pub fn add(left: usize, right: usize) -> usize {
    left + right
}

#[cfg(test)]
mod tests {
    use super::*;

    #[test]
    fn it_works() {
        let result = add(2, 2);
        assert_eq!(result, 4);
    }
}
